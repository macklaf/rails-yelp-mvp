<div class="container">
  <h1> New Comment for <%= @restaurant.name %> </h1>

  <%= simple_form_for [ @restaurant, @review ] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: Review::RATE  %>
    <%= f.submit :post_comment, class: "btn btn-primary"%>
  <% end %>
</div>
